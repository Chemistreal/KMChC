# 결과 수집 설치 가이드 (Apps Script) — 사전지식 0에서 시작

> ⚠️ **레거시 문서(v1)** — 이 가이드의 `apps_script.gs`·`makeSummary`·저장 항목은 **구버전**입니다. 현행 백엔드는 `apps_script/AppsScript_GitHub빠른조회_정리본.gs`(31열, ID·짧은리포트주소·JSONP·GitHub 발행)이며, **이 문서대로 구 코드를 붙여넣으면 현행 배포가 구버전으로 덮어써집니다.** 신규 설치·수정은 HANDOFF.md §1을 따르세요.

## 이게 뭔가요?
구글 시트에 붙는 아주 작은 프로그램입니다. 역할은 하나 — 학생이 진단을 제출할 때마다
그 결과를 **시트에 한 줄씩 자동으로 받아 적는 '우편함'**입니다. 앱이 그 우편함 주소로 결과를 보냅니다.

## 꼭 해야 하나요? → 아니요, 선택입니다
- 진단 풀기 / 리포트 보기 / 결과 링크 공유는 **이거 없이도 100% 작동**합니다.
- Apps Script는 **"모든 학생 결과를 한 시트에 모으고 싶을 때"만** 필요합니다.
- 지금 안 해도 되고, 나중에 붙여도 됩니다. (비워두면 앱은 그냥 전송을 건너뜁니다.)

## 모으고 싶다면 — 6단계
1. **빈 구글 시트 만들기**: sheets.google.com → "빈 스프레드시트"
2. 상단 메뉴 **[확장 프로그램] → [Apps Script]** (새 탭에 편집기가 열림)
3. 편집기의 **기존 코드를 모두 지우고**, `apps_script.gs` **내용을 통째로 붙여넣기** → 저장(Ctrl/⌘+S)
4. 오른쪽 위 **[배포] → [새 배포]** → 톱니바퀴 ⚙️ → **"웹 앱"** 선택
   - 실행 계정: **나**
   - 액세스 권한: **모든 사용자**   ← 반드시 이것
   - **[배포]** → 권한 요청 시: *고급 → (프로젝트)로 이동 → 허용*
   - 마지막에 나오는 **웹 앱 URL 복사** (…/exec 로 끝납니다)
5. `index.html`을 편집기로 열고, 위쪽 `<script>` 안의
   `var APPS_SCRIPT_URL = "";`
   를 찾아 **따옴표 사이에 그 URL을 붙여넣기** → 저장
   (선택) 보안을 더하려면 `var SHARE_TOKEN = "원하는암호";` 를 정하고,
   `apps_script.gs` 의 `var TOKEN = "";` 도 같은 값으로 맞추세요.
6. **확인**: 앱을 열어 데모나 실제 제출을 한 번 해본 뒤, 시트를 보면 "결과" 줄이 새로 생겨 있습니다.

## 자주 막히는 곳
- 액세스 권한이 "모든 사용자"가 아니면 전송이 안 됩니다.
- 코드를 고친 뒤에는 **[배포 관리] → 연필(수정) → "새 버전"**으로 다시 배포해야 반영됩니다.
- 시트에 안 쌓이면: URL이 …/exec 인지, APPS_SCRIPT_URL 따옴표 안에 정확히 들어갔는지 확인하세요.

## 보너스 — 반별 요약
Apps Script 편집기에서 `makeSummary` 함수를 한 번 실행하면,
"요약" 시트에 **유형 분포**와 **직관 밴드(양호/흔들림/교정필요) 집계**가 자동으로 만들어집니다.

## 저장되는 항목(결과 시트)
시각 · 이름 · 학년 · 유형 · 유형명 · 흥미 · 효능감 · 정서안정 · 가치 · 심층 · 표층 · 메타인지 ·
개념직관 · 보존 · 입자 · 변화분류 · 고착(우선교정) · 빈칸(지식부족) · 데이터품질 · 소요(초) ·
응답원본(복원용) · 리포트링크
